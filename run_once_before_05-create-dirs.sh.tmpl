#!/bin/bash

# Exit on error
set -e

# Create local directories for user scripts
DIRECTORIES="$HOME/.local/cli/bin"

for dir in $DIRECTORIES; do
    if [ ! -d "$dir" ]; then
        echo "Creating directory: $dir"
        mkdir -p "$dir"
    else
        echo "Directory already exists: $dir"
    fi
done
